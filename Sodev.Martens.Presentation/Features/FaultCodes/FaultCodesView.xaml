<UserControl x:Class="Sodev.Marten.Presentation.Features.FaultCodes.FaultCodesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sodev.Marten.Presentation.Features.FaultCodes"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Fault codes detected:" />
            <TextBlock Text="{Binding FaultCodesNumber}" />
        </StackPanel>

        <ListView ItemsSource="{Binding FaultCodesList}"></ListView>
        
        <Button Grid.Column="1" Grid.Row="4" Margin="10, 0"
                    Content="Request fault codes" cal:Message.Attach="RequestFaultCodes" />
        <Button Grid.Column="1" Grid.Row="4" Margin="10, 0"
                Content="Clear fault codes" cal:Message.Attach="ClearFaultCodes" />
    </StackPanel>
</UserControl>
